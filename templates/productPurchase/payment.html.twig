{% extends 'clerk/baseClerk.html.twig' %}

{% block head %}
    {{ parent() }}
{% endblock %}
{% block body %}
    <h1>Payment</h1>
    <form id="productForm" class="form" method="POST" action="/product/payment">
        {% if method == 'Grynais' %}
        <div class="form-group m-3">
            <label for="money">Amount paid:</label>
            <input type="number" class="form-control" id="money" name="money" />
        </div>
        {% elseif method == 'Kortele' %}
        <div class="form-group m-3">
            <label for="card_number">Card number:</label>
            <input type="text" class="form-control" id="card_number" name="card_number" />
        </div>
        {% else %}
        <div class="form-group m-3">
            <label for="money">Cash amount:</label>
            <input type="number" class="form-control" id="money" name="money" />
        </div>
        <div class="form-group m-3">
            <label for="code">Gift code:</label>
            <input type="text" class="form-control" id="code" name="code" />
        </div>
        {% endif %}
        <button type="submit" class="mt-3 mr-2 mb-3 btn btn-primary">Confirm payment</button>
    </form>
{% endblock %}